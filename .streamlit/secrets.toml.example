# Streamlit Secrets Configuration
# ===================================
# This file is for LOCAL DEVELOPMENT ONLY (when running without Docker)
# For Docker: All configuration comes from docker-compose.yml environment variables
#
# To use for local dev:
# 1. Copy this file to .streamlit/secrets.toml
# 2. Fill in your actual values
# 3. Never commit secrets.toml to git (already in .gitignore)

# Spotify API Keys (if testing Spotify integration directly)
# SPOTIFY_CLIENT_ID = "your_client_id_here"
# SPOTIFY_CLIENT_SECRET = "your_client_secret_here"

# NOTE: API_BASE_URL should be set as an environment variable, not here
# For local dev without Docker: export API_BASE_URL=http://localhost:8001
